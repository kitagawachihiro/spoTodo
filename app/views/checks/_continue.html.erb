<%= link_to todo_path(id:todo.id, update_branch: "finish"),
    id: "js-check-button-for-todo-#{todo.id}",
    method: :patch,
    remote: true do %>

    <%= icon 'far', 'square fa-lg' %>

<% end %>
